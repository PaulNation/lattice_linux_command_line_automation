# Machine-local tool paths — never commit
_automation/paths.cfg

# Diamond GUI project artifacts (generated at runtime, not scaffolded)
*/prj/

# Project export archives and intermediate files
*/export/
